input_right = keyboard_check(ord("D")); // right
input_left = keyboard_check(ord("A")); // left
input_jump = keyboard_check_pressed(ord("W")); // jump
input_dash = keyboard_check(vk_shift); // run faster
input_shoot = (vk_left); // attack
input_fly = keyboard_check(vk_space); // fly
input_release_fly = keyboard_check_pressed(vk_space); // fly